//! The domain crate contains pure domain types, entities, value objects, and
//! aggregates. It has no dependencies on any other crate in the workspace, no
//! async, and no I/O. All domain rules and invariants are enforced here.
