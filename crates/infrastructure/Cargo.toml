[package]
name = "infrastructure"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
domain = { path = "../domain", version = "0.1.0" }
application = { path = "../application", version = "0.1.0" }

[lints]
workspace = true
